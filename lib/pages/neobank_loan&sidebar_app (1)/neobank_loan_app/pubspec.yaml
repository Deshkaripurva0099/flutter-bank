name: neobank_loan_app
description: A new Flutter project for loan management.

publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  
  cupertino_icons: ^1.0.2
  intl: ^0.18.1              # For currency and date formatting
  provider: ^6.1.1           # State management
  shared_preferences: ^2.2.2 # Local storage
  flutter_svg: ^2.0.9        # SVG support (optional)
  
dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^2.0.0

flutter:
  uses-material-design: true

  # Uncomment if you need assets
  # assets:
  #   - images/
  #   - icons/